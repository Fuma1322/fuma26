import React from "react";
import HeroContent from "../sub/HeroComponent";

const Hero = () => {
  return (
    <div className="relative flex flex-col h-full w-full" id="about-me">
      <HeroContent />
    </div>
  );
};

export default Hero;